# JusticeLens AI -- Backend module dependencies
# (data_loader / data_validation / feature_engineering / data_engineering / utils / logger / config)

pandas>=2.0
numpy>=1.24
requests>=2.28

# Optional: enables Parquet I/O in utils.save_dataframe / load_dataframe.
# Without it, the utilities transparently fall back to CSV.
pyarrow>=14.0

# Required by later pipeline stages (modeling / explainability) per the
# finalized architecture -- not imported by the modules in this delivery,
# listed here for environment completeness.
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44
